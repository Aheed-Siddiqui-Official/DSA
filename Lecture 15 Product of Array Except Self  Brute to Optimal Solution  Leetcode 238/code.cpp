#include <iostream>
#include <vector>
using namespace std;

int main() {

                                        // BRUTE FORCE

    // vector<int> vec = {1, 2, 3, 4, 5};
    // vector<int> ans(vec.size(), 1);

    // for(int i = 0 ; i < vec.size() ; i++) {
    //     for(int j = 0 ; j < vec.size() ; j++) {
    //         if(i != j) {
    //             ans[i] *= vec[j];



    

    return 0;
}