#include <iostream>
#include <vector>
using namespace std;

int peakMountain(vector<int> &vec) {
    int st = 1;
    int end = vec.size() - 2;

    
    return -1;
}

int main() {
    vector<int> mountain = {0, 3, 8, 9, 5, 2};
    cout << "Peak Value = " << peakMountain(mountain);
    return 0;
}